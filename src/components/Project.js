// the component function
const Project = (props) => {
  //The Components Returned JSX
  return <div className="ComponentName"> This is Project</div>;
};

// export the component
export default Project;
